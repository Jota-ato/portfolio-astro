<article class="flex flex-col md:flex-row gap-8"  {...Astro.props}>
  <div class="shrink-0 max-w-xl [&>img]:rounded-md [&>img]:aspect-video">
    <slot name="image" />
  </div>
  <div class="flex-1 flex flex-col gap-4">
    <slot name="header" />
    <slot name="body" />
  </div>
</article>
